CREATE TABLE last_check (
    blockheight INTEGER
);
INSERT INTO last_check (blockheight) VALUES (46677180);

CREATE TABLE stakes (
    block INTEGER,
    blockid TEXT,
    timestamp REAL,
    user TEXT,
    asset TEXT,
    stakelength TEXT,
    stakevalidtime REAL,
    amount REAL,
    earlyamount REAL
);

CREATE TABLE cancelledstakes (
    block INTEGER,
    blockid TEXT,
    timestamp REAL,
    user TEXT,
    asset TEXT,
    stakelength TEXT,
    stakevalidtime REAL,
    amount REAL,
    cancelledtime REAL
);

CREATE TABLE payouts (
    blockid TEXT,
    account TEXT,
    stakeamount REAL,
    stakelength TEXT,
    payoutamount REAL,
    timestamp REAL
);

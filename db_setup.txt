CREATE TABLE last_check (
    blockheight INTEGER
);
INSERT INTO last_check (blockheight) VALUES (56800275);

CREATE TABLE stakes (
    block INTEGER,
    blockid TEXT,
    timestamp REAL,
    user TEXT,
    asset TEXT,
    stakelength TEXT,
    stakevalidtime REAL,
    amount REAL
);

CREATE TABLE payouts (
    blockid TEXT,
    account TEXT,
    stakeamount REAL,
    stakelength TEXT,
    payoutamount REAL,
    timestamp REAL
);
